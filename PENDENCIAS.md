# Pendências

Itens acordados/latentes com contexto e ponteiro. Resolver = apagar a linha no mesmo commit do fix.

- **[2026-07-14] Fase 2 Nibo — normalização na ingestão:** quando o adapter Nibo existir (`supabase/functions/sync-omie/index.ts:444` hoje fail-closa `provedor !== 'omie'`), avaliar normalizar contraparte/categoria UMA vez (adapter ou `MovimentosProvider`) em vez de rotear por `chaveContraparte` em cada consumidor — a revisão de 2026-07-14 achou `src/core/filial.ts` fora da migração (corrigido, mas o próximo consumidor nasce com o mesmo risco).
- **[2026-07-14] GUID real sem cadastro vaza como "nome":** `nomeContraparte` (src/core/cliente.ts) devolve a chave crua quando não é numérica — stakeholder Nibo com GUID real fora do cadastro (drift movimentos×cadastros) apareceria como GUID; o nome legível existe em `m.contraparte` mas a função não o recebe. Espelho em `resolvedor.ts` (`descCategoria.get(codigo) ?? codigo`). Decidir na Fase 2: passar o nome do movimento ou reusar `RE_CODIGO_OPACO` com marcador "sem cadastro".
- **[2026-07-14] CPF/CNPJ no campo `contraparte` (Omie):** o pipeline Omie grava `cCPFCNPJCliente` em `m.contraparte` — se algum dia vier doc sem `nCodCliente` (0/408 no seed atual), o documento vira rótulo/chave via fallback de `chaveContraparte`. Na Fase 2, definir semântica única do campo por provedor (Nibo = nome, Omie = doc) ou mascarar.
- **[2026-07-14→22] `RelatorioPrevistoRealizado` infere natureza por prefixo `'1'/'2'`** (src/components/relatorios/RelatorioPrevistoRealizado.tsx) — MOOT para Nibo: esse trecho só percorre o doc de orçamento, que só o Omie produz (códigos hierárquicos). Vira problema real se algum dia existir provedor com orçamento e categoria opaca.
- **[2026-07-14] Edge `sync-omie` não checa `res.ok` dos upserts** (gravações em `Promise.all`, ~linhas 357-363/458-463) — falha parcial de escrita é engolida e gera drift silencioso entre `movimentos-raw` e `cadastros-raw`.
- **[2026-07-14→23] Overrides de contraparte por nome cru — risco MEDIDO como zero na frota atual:** auditoria 2026-07-23 (23 clientes, 111k movimentos): TODO movimento Nibo tem `stakeholder.id` e nos 3.754 casos Omie sem código o nome também é vazio (balde `SEM`, estável). O fallback nome-cru em `chaveContraparte` segue no código e volta a ser risco se algum provedor futuro emitir nome sem código — reavaliar a cada provedor novo.
- **[2026-07-16] Acesso-cliente AUTAG — faltam 2 passos:** tabela `painel_acessos` + RLS role-aware já estão EM PROD e verificadas (migration `2026_07_16_acesso_cliente_rls.sql`; front `useAcesso`+gate). Falta: (1) criar o usuário Supabase do AUTAG e inserir os vínculos `painel_acessos` (papel cliente → `a17a9000-0000-4000-8000-000000000001` [36] e `1e8607b5-a9ef-4e60-996f-cab4ba22c00e` [27]); (2) `RelatorioDRE`/`RelatorioDFC` mostram "Exportar CSV" no HUD do cliente — se o cliente não deve exportar, flag `semExport` nos componentes.
- **[2026-07-16] ids de tenant STALE em `core/tenant.ts`:** `ACME_ID`/`ACME_27_ID` (…036/…027) ≠ prod (`a17a9000-…0001` / `1e8607b5-…c00e`). O app funciona por fallback (`clientes[0]`), mas `PISO_DADOS_POR_CLIENTE` (piso mai/2026) está keyado nos ids errados → o piso de dados do AUTAG NÃO está sendo aplicado. Corrigir os constantes p/ os ids reais.
- **[2026-07-16] Lixo de fuzzing em `painel_estado`:** chaves `poison` (25 docs), `xss_probe`, `test_insert`, `i024`, `i043`, `` (vazia) — resíduo da RLS aberta. Invisíveis ao cliente (não casam `cliente:<id>:`), mas poluem a base. Limpar com `delete ... where chave not like 'cliente:%' and chave not in (...legítimas)` após conferir cada uma.
- **[2026-07-16] Truncamento de sync só avisa no log:** `avisarSeTruncado` (supabase/functions/sync-omie/index.ts) grita no log da edge quando bate `MAX_PAGINAS`, mas o `HistoricoSync` na UI não sabe. Cliente com >50k movimentos veria DRE parcial sem sinal na tela. Fase futura: propagar um flag `truncado` no retorno do sync até o painel de conferência (contrato de resposta — mesma cautela do item de `res.ok`).
- **[2026-07-22] NIBO — data de baixa aproximada:** o `/schedules` não expõe a data real do pagamento, então movimento pago usa `dataPagamento = dueDate` (auditado em `campoValor='nibo:baixa~vencimento'`). Afeta o **recorte mensal** da DFC (um título vencido em jan e pago em fev entra em jan), não os totais. Refinar com o extrato (`/entries`) se o cliente cobrar precisão mensal de caixa.
- **[2026-07-22] NIBO — `/costcenters` nunca exercitado:** a DR PIZZA tem 0 centros de custo, então `mapearCostCenters`/`centroPrincipal` foram deployados sem dado real. O 1º cliente NIBO COM centro de custo precisa de conferência do shape (`id`/`costCenterId`, `description`/`name`) antes de confiar no rateio por filial.
- **[2026-07-21] sync-omie estoura o edge (150s) em cliente grande:** Magdiel Lanches (24.323 movimentos, 244 págs a 100/pág — Omie trava página em 100) não sincroniza pela edge — `ListarMovimentos` pagina SEM filtro de data (busca TUDO e filtra `desde` só depois). Onboarding feito rodando a edge localmente via Deno (~10min). Fix p/ syncs recorrentes: passar `dDtPeriodoInicial/Final` à Omie no ListarMovimentos (incremental real, poucas páginas) ou mover o corpo pro `EdgeRuntime.waitUntil` (bg ~4,5min).
- **[2026-07-22] `Previsto × Realizado` fica sem a coluna Realizado no Nibo:** as colunas Previsto e Realizado saem do doc de orçamento, que só o Omie produz — a tela agora AVISA isso em vez de fingir, mas o cliente Nibo tem baixas reais (727 na DR PIZZA) que só aparecem na DFC. Opção a decidir: derivar o Realizado das baixas (`movimentosCaixa`, por `dataPagamento`) quando não há doc de orçamento. Não implementado porque muda a semântica de "Realizado" (hoje = baixa auditável 1:1 no ERP) e inclui transferências que a Regra Mãe excluiria.
- **[2026-07-23] Linha fantasma em painel_estado:** `cliente:00000000-0000-4000-8000-000000000036:painel-ag-overrides-v1` — origem dos 6 renomes já migrados para a DR PIZZA. Mantida como evidência do bug; apagar quando não for mais útil (junto com a limpeza do lixo de fuzzing).
- **[2026-07-24] Multi-acesso + realtime PLANEJADO (não implementado):** plano completo em `docs/PLANO_MULTI_ACESSO.md` — 5 fases (rev+CAS+merge por domínio · tabela-sinal p/ realtime · papel analista com carteira). Motivador urgente: escrita concorrente de 2 operadores hoje é last-write-wins no doc inteiro (perda silenciosa). A parte do `salvarRemoto` engolir erro foi RESOLVIDA em 2026-07-29 (selo de sincronização na Topbar, PR #11) — o que resta da Fase 1 é o CAS/merge por domínio contra o last-write-wins.
- **[2026-07-24] Apresentacoes mensais por cliente PLANEJADO:** `docs/PLANO_APRESENTACOES.md` — tabela `painel_apresentacoes` (competencia + status + versao), congela AGREGADOS (~30 kB) e nao o snapshot (512 kB); publicacao controlada pelo operador via RLS. Inclui otimizacao independente: `apresentacaoAutonoma.ts:20` embute TODOS os movimentos do cliente no HTML (deveria filtrar pela competencia — corta ~90%).
- **[2026-07-24] `apresentacao-comentarios-v1` e resíduo:** 2 docs em prod, zero referencias no codigo. Limpar junto com o lixo de fuzzing.
- **[2026-07-24] Apresentacoes — publish atomico:** publicar/republicar hoje depende de ordem correta no app (arquivar atual -> inserir versao max+1) com read-modify-write em `versao`. Schema sustenta, mas e foot-gun sob 2 operadores (23505 na corrida). Encapsular em `public.publicar_apresentacao(...)` SECURITY DEFINER (transacao + lock + retry) ao construir a camada de publish.
- **[2026-07-24] `tocar_atualizado_em()` tem nome generico** e foi criada com `create or replace` — hoje so 1 trigger depende dela e o corpo e identico, mas migration futura pode sobrescrever. Fixar contrato via `comment on function` ou renomear.
- **[2026-07-25] RH: welcome-email consertado por tabela, nao re-testado fim-a-fim:** a RESEND_API_KEY do RH em prod nao era uma chave Resend (welcome quebrado silencioso); a chave nova ja esta nos secrets do RH (le em runtime, sem redeploy). Falta um teste fim-a-fim NO RH (cadastrar cliente com convite) para fechar.
- **[2026-07-25] Template `apresentacao` sem botao na UI:** a edge `enviar-email` ja aceita anexo HTML; o botao "Enviar por e-mail" entra junto com a Fase 1/2 do PLANO_APRESENTACOES (publicacao), que e quem sabe a competencia e o arquivo.
- **[2026-07-25] 2FA: falta o teste fim-a-fim com login real:** o gate foi provado por SQL e a edge recusa sem sessao, mas o caminho completo (senha -> e-mail chega -> codigo libera) so pode ser exercitado por quem tem a senha. Ricaliff precisa entrar uma vez e confirmar.
- **[2026-08-02] Desligar o site Netlify legado:** a migração pro Vercel (decisão 26/07) ESTÁ CONCLUÍDA — prod real = projeto Vercel `pulsar-finance` (git-conectado, auto-deploy na main) em `pulsar-finance.agconsultorialtda.com` (COM hífen; via Cloudflare), verificado 02/08 servindo o bundle do dia (`index-CiqPGAOF.js`, marcador CAPEX). O site Netlify `cosmic-entremet-360337` segue NO AR servindo build velho (créditos de build esgotados lá; deploy de 02/08 recusado) — desligar/arquivar pra ninguém abrir a versão defasada por engano. O domínio sem hífen (`pulsarfinance.`) não responde mais.
- **[2026-08-02] Vulnerabilidades dev-only (esbuild/vite ≤6):** `npm audit` acusa 2 (1 high na cadeia do vite) — afetam só o DEV SERVER, não o artefato estático em prod. Fix é breaking (vite 8): fazer upgrade + rodar a suíte + conferir o export HTML (bundle único) numa sessão própria.
- **[2026-08-02] DFC recortada pelo regime global (competência por padrão):** `useResultado.ts` filtra o período com o regime único do seletor global antes de `movimentosCaixa` — com o seletor em competência (padrão), o recorte mensal da DFC ancora por data de EMISSÃO e um título pago fora do mês de emissão cai no mês errado do fluxo, embora o cabeçalho afirme "regime caixa". Mais amplo que o item Nibo de 22/07. Decidir: DFC sempre recortar por `dataPagamento` (ignorando o seletor) ou manter acoplado e ajustar a copy. Mesma acoplagem em `useComparativo.ts` e `demoMensal.ts`.
- **[2026-08-02] Previsto × Realizado ignora o filtro de filial:** o relatório lê `useMovimentos()` direto; o SeletorFilial aparece acima dele mas nada muda (orçamento e baixas seguem consolidados). Escopar por filial exige decidir como ratear o doc de orçamento (que não tem filial) — ou esconder o seletor nessa vista com aviso.
- **[2026-08-02] HTML exportado abre com fonte do sistema offline:** o CSS inlinado mantém `url(/assets/inter-*.woff2)` absoluto → 404 em `file://`. `inlinarFontes` só cobre Google Fonts (que o index não usa = código morto) e `comoDataUri` não checa `res.ok` (corrupção silenciosa se algum dia reativado). Fix: embutir os woff2 do próprio host como data URI no passo de inline do CSS.
- **[2026-08-02] Formatação percentual fora do money.ts:** `indicadores.ts:111` e `useContagem.ts:43` usam `toLocaleString` inline p/ percentual — extrair um `pctSimples(valor, casas)` pro `money.ts` (padrão formatadores no módulo único).
- **[2026-08-03] Auditoria de auth — itens MÉDIOS não corrigidos (backlog):** (1) `convidar-acesso` não está no `config.toml` — o `verify_jwt=true` está ativo em prod mas o contrato não é versionado (um redeploy sem a flag transformaria o `atob` de claims em bypass); (2) `enviar-email` aceita destinatário e anexo livres — restringir a e-mails já cadastrados; (3) senha mínima de 6 caracteres e inconsistente entre telas (RecuperarSenha exige 8); (4) 2FA sem teto por USUÁRIO (só por sessão/desafio) — múltiplos logins multiplicam as tentativas; (5) `painel_sessoes_2fa`/`painel_dispositivos_confiaveis` ainda concedem INSERT/UPDATE/DELETE a `authenticated` (hoje protegido só por policy — revogar grant seria fail-closed de verdade); (6) token de aparelho confiável sobrevive ao logout e não há "esquecer este aparelho"; (7) funções `cliente_ids_do_usuario`/`eh_operador` executáveis por anon (enumeração de vínculos); (8) esquema legado de tenancy (`lancamentos`/`categorias`/`tenants`) sem gate de 2FA — hoje fail-closed para anon (provado), mas fora do padrão.
- **[2026-08-03] Convite para e-mail ERRADO entrega acesso a estranho:** a conta nasce vinculada ao tenant ANTES de qualquer confirmação — um typo do operador no e-mail dá ao dono daquele endereço um login legítimo do cliente (com 2FA no inbox dele). Mitigação a decidir: confirmação dupla do e-mail no formulário, ou vincular só após o primeiro login bem-sucedido.
- **[2026-08-03] Cliente AUTAG ainda sem acesso utilizável:** o convite por e-mail real FUNCIONA desde 03/08 (trigger removido — ver migration `2026_08_03_signup_nativo_sem_trigger.sql`; signup público segue bloqueado pelo `disable_signup` nativo do GoTrue, provado empiricamente). Falta o passo humano: convidar o e-mail REAL do dono do AUTAG pelo painel (Acessos → Convidar por e-mail) — a conta sintética `autag@` não recebe 2FA e deve ser aposentada depois.
