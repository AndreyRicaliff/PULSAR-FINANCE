/**
 * @file Fundo do login — v3 sóbrio: cena decorativa aposentada.
 * O candlestick de pregão, orbes, EKG e piso de radar 3D eram da estética "console" pré-v3
 * (neutralizada). Em v3 o login é o fundo limpo (surface) + o card; sem decoração em movimento.
 */
export function CenaPulso() {
  return null
}
