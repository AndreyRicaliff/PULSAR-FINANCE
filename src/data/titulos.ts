import type { TitulosSeed } from '@/core/titulo'

// Seed vazio: títulos vêm do Supabase em runtime (JSON real fica fora do git).
export const titulosSeed: TitulosSeed = { geradoEm: '', titulos: [] }
